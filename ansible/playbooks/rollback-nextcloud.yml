---
- name: Rollback Nextcloud - suppression applicative
  hosts: zt-app
  become: true

  vars:
    nextcloud_install_dir: "/var/www/nextcloud"
    nextcloud_data_dir: "/var/www/nextcloud-data"

  tasks:

    - name: "🛑 Arrêt Apache"
      service:
        name: apache2
        state: stopped

    - name: "🧹 Suppression application Nextcloud"
      file:
        path: "{{ nextcloud_install_dir }}"
        state: absent

    - name: "🧹 Suppression répertoire de données"
      file:
        path: "{{ nextcloud_data_dir }}"
        state: absent

    - name: "🧪 Vérification suppression"
      stat:
        path: "{{ nextcloud_install_dir }}"
      register: nc_removed

