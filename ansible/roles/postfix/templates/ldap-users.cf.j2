# LDAP lookup for Postfix
server_host = {{ ldap_server }}
search_base = {{ ldap_base_dn }}
query_filter = (mail=%s)
result_attribute = mail
bind = yes
bind_dn = {{ ldap_bind_dn }}
bind_pw = {{ ldap_bind_password }}
version = 3
